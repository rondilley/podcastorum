faster-whisper>=1.0.0   # preferred whisper backend (CTranslate2, supports CUDA + ROCm v4.7+)
openai-whisper>=20231117  # fallback whisper backend (PyTorch, works on any GPU backend)
anthropic>=0.40.0
openai>=1.0.0
feedparser>=6.0.0
requests>=2.28.0
